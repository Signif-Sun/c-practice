#include <stdio.h>


int main(void)
{
	int  a, b, c;
	puts("pls input three nums:");
--------------------------scanf作为判断的时候，中间加‘，’输入一定要加‘，’，并且输入几个数字scanf值就要等于几---------- 
// 当我需要三个数字，我的scanf!=3的时候，一定不会执行while循环，结束了

	//while (scanf_s("%d,%d,%d",&a,&b,&c) == 3)
  
  while (scanf_s("%d %d %d",&a,&b,&c) == 3)
	{
		printf("the sum is %d.",a + b + c);
	}

	return 0;
}
